import styled from "styled-components";

export const TipsStyleContainer = styled.div`
    position: absolute;
    color: white;
    top: calc(1rem + 6rem);
    left: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;


    p {
        display: flex;
        gap: 1rem;
        font-weight: 100;
        font-size: 0.75rem;
    }
`